# **MUST** use uri versioning

Path **MUST** contain a `MAJOR` version segment.

## Invalid Example

```yaml
/user:
```

## Valid Example

```yaml
/user/v1:
```

[UKHSA Guidelines Versioning][1]

[1]: ../../api-guidelines/versioning-and-deprecation.md#uri-versioning
