extends:
  - '@commitlint/config-conventional'
rules:
  type-enum: [2, 'always', [
    'build',
    'docs',
    'feat',
    'fix',
    'perf',
    'refactor',
    'revert',
    'test'
  ]]
  scope-enum: [2, 'always', [
    'linting-rules'
  ]]
  scope-empty: [0, 'never']
